# Only needed if the frontend and backend are deployed on different domains.
# When the backend serves the built frontend itself (the default - see
# backend/main.py), leave this unset and requests use the same origin.
VITE_API_BASE=https://your-backend-domain.com/api
